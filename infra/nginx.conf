server {
    listen 80;
    location / {
        default_type text/plain;
        return 200 "TECHCORP - service web disponible\n";
    }
}
# Témoin indépendant du port principal pour démontrer SERVICE_DOWN.
server {
    listen 8081;
    location / {
        default_type text/plain;
        return 200 "TECHCORP - temoin reseau\n";
    }
}
